def multiply(number_one, number_two):
	negative_result = (number_one < 0) ^ (number_two < 0)
	number_one, = abs(number_one), abs(number_two)
	number_two 
    	result = 0
    	for _ in range(number_two):
        	result += number_one

    	return -result if negative_result else result
